# Contracts

This directory contains human-readable and machine-readable examples of cross-service contracts.

The canonical contract boundary is between the ASP.NET control plane and Python AI runtime. The contract is project-owned and must not expose framework-specific types.

Issue 002 will add concrete request/response examples and, if useful, schema artifacts.
