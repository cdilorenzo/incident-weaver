# Deployment verification checklist

After deployment, verify the expected version on every instance, readiness status, startup logs, and representative request behavior. Note any instance that reports a different version or fails initialization.

Deployment verification is evidence gathering, not authorization to roll back or restart a service.