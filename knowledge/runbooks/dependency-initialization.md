# Dependency initialization failures

Startup dependency failures commonly appear as an initialization error followed by a readiness or server-start failure. The useful correlation fields are service, instance, release version, and timestamp.

Record the observed failure and its scope. Do not infer that all instances are affected without checking health evidence.